# PostgreSQL (e.g. Supabase). Get from Supabase project Settings > Database.
DATABASE_URL="postgresql://user:password@host:5432/postgres"

# JWT signing secret for session cookies (use a long random string in production).
JWT_SECRET="your-secret-at-least-32-chars"
